export const BACKEND_ORIGIN = "http://localhost:1001";
export const FRONTEND_ORIGIN = BACKEND_ORIGIN;

export const LOGIN_URI = `${BACKEND_ORIGIN}/login`;
export const LOGOUT_URI = `${BACKEND_ORIGIN}/logout`;

export const REGISTER_URI = `${BACKEND_ORIGIN}/register`;

export const AUTH_CHECK_URI = `${BACKEND_ORIGIN}/auth/user`;

// export const CSRF_URI = `${BACKEND_ORIGIN}/sanctum/csrf-cookie`;